//List the enum
//They cannot create inside the class

enum TextSizes { small, medium, large }

enum OrderStatus { processing, shipping, delivery}

enum PaymentMethods { paypal, googlePay, applePay, visa, masterCard, creditCard, payStack, razorPay, paytm, aba}